# [PRD v2 초안] IB 부서 원스톱 성적 변환 · 성적표 발급

작성일: 2026-09-16 (인터뷰 결과 정리)  
수정일: 2026-09-17 (나이스 샘플·도덕 평가계획 확인 후 반영, 1차 구현)

## 1. 바뀌는 점 요약

| 항목 | v1 (현재 배포본) | v2 |
|---|---|---|
| 사용자 | 교과 교사 | **IB 코디네이터 · 계원 교사** (교과·담임 교사 업무 없음) |
| 점수 입력 | 교사가 과목별 직접 입력 | **나이스 성적 파일 업로드** (반 단위) |
| 변환 기준 | 앱에서 과목마다 설정 | **엑셀 기준 템플릿 업로드** (평가계획 IB 가이드를 계원이 옮겨 적음) |
| 성적표 | 학생 × 과목마다 1장 | **학생 1명당 전 과목 1부** (한 학년 10과목 이상 → 2쪽 이상 허용, 쪽마다 학생 식별 머리글 반복) |
| 교사 의견 | 입력란 있음 | **선택** (체크하면 학생당 1개 작성·출력) |
| 등급 설명 | 1개 등급 | 학생 성적표에 나온 등급별로 해당 과목과 함께 표시 (선택) |
| 직접 입력 | 주 기능 | **보조 기능** (업로드 후 특정 학생·과목 점수 수정) |
| 서버 | 없음 | 없음 (그대로 유지, 파일은 브라우저 안에서만 읽음) |

## 2. 작업 흐름

1. **기준 템플릿 업로드**: 학년·과목별 변환 규칙이 담긴 엑셀 파일 (학기 초에 한 번 작성, 학기 말에 재사용)
2. **나이스 성적 파일 업로드**: 과목 × 강의실 단위 파일 (여러 개 한 번에)
3. **검토**
   - 템플릿에 없는 과목·영역, 규칙 구간에 안 맞는 점수, 결측값 경고
   - 학생 × 과목 결과표 (Criterion A~D, 총점, 등급)
   - 필요 시 개별 점수·수준 수정 (보조 기능)
4. **성적표 일괄 출력**: 반 전체 인쇄 / PDF 저장

## 3. 변환 규칙 (인터뷰 결정 사항)

- 가이드 형태는 **과목마다 섞여 있음** → 규칙을 행 단위로 자유롭게 조합할 수 있어야 함
  - 영역 1개 → 기준 1개
  - 영역 1개 → 기준 여러 개: **기준마다 구간표가 따로** 있음
  - 여러 영역 → 기준 1개
- 지필평가 반영 여부: **과목마다 다름** (규칙에 지필 항목이 있으면 반영, 없으면 미반영)
- 같은 기준에 근거가 여러 개일 때 계산 방식: **과목별로 지정** (일관된 수준 / 최근 / 최고 / 평균, 기본 일관된 수준). 계산값은 교사 판단을 돕는 제안값 (7장)
- **한 영역 → 기준 여러 개일 때 나이스에는 영역 합계만 있음** (예: 과학윤리 글쓰기 25점 = B 10점 + D 15점)
  - 합계가 되는 배점 조합 중 **기준별 점수 차가 가장 작은 조합**을 고름 (예: 23 → B10·D13, 21 → B10·D11)
  - 조합이 여러 개였으면 검토 화면에 "추정" 표시, 조합이 없으면(예: 기타 배점 8·7점) "확인 필요"로 두고 수동 수정
- 배점 하나가 수준 범위에 대응할 때(예: 10점 → 7~8): **높은 쪽** 기본값 (설정에서 낮은 쪽 선택 가능)
- 4개 기준을 모두 평가하지 않은 과목: **기본은 등급 없이 수준만 보고** (IB 규정). 학교 방침으로 환산 등급을 켤 수 있음 (7장)
- 등급 경계: MYP 기본값 1–5, 6–9, 10–14, 15–18, 19–23, 24–27, 28–32

## 4. 기준 템플릿 구조 (확정, 도덕 평가계획 기준)

**시트 `과목`**: 과목당 1행

| 학년 | 나이스 과목명 | MYP 과목군 | 성적표 표시명 | 근거 합치는 방식 |
|---|---|---|---|---|
| 3 | 도덕 | 개인과 사회 | 도덕 | 평균 |

**시트 `규칙`**: 평가계획 채점 기준표의 배점 1칸당 1행

| 학년 | 나이스 과목명 | 나이스 영역명 | Criterion | 배점 | 수준 |
|---|---|---|---|---|---|
| 3 | 도덕 | 과학윤리 글쓰기(논술형) | B | 10 | 7~8 |
| 3 | 도덕 | 과학윤리 글쓰기(논술형) | B | 8 | 5~6 |
| 3 | 도덕 | 과학윤리 글쓰기(논술형) | D | 15 | 7~8 |
| 3 | 도덕 | 과학윤리 글쓰기(논술형) | D | 7 | 0 |

- 배점은 한 점수(`10`) 또는 구간(`18~20`), 수준은 한 값(`8`) 또는 범위(`7~8`)
- 영역 만점은 기준별 최고 배점의 합으로 계산해 나이스 만점과 대조
- 템플릿은 앱에서 **빈 양식 내려받기**로 제공 (시트: 안내, 과목, 규칙, 목록)
- 업로드 시 검사: 머리글, 학년·과목군·방식·Criterion·배점·수준 형식, 과목 시트에 없는 과목, 배점 구간 겹침(오류)·빈틈(경고)

## 4-1. 나이스 파일 구조 (샘플 확인)

- 메뉴: 수행평가 강의실별 일람표, **XLS data**로 받은 .xlsx (병합 셀 없음)
- 파일 1개 = 과목 1개 × 강의실 1개
- 3행 `2026학년도 2학기 주간 3학년 1 강의실`, 4행 `교과목 : 도덕`
- 5행 머리글: `반/번호 | (학번, 머리글 없음) | 성명 | 영역명(만점 25.00,25.00%) … | 합계 | 비고`
- 6행부터 학생 1명당 1행, 끝에 응시생수·총점·평균 요약 행
- 학생은 학번으로 여러 파일을 합침

## 4-2. 완성본 예시 (시연용)

- 3학년 가상 2개 반(반당 12명), 11과목 (MYP 1~3년차는 8개 과목군을 모두 배워야 하므로 디자인 과목군으로 기술·가정 추가). 상단 **완성본 예시** 버튼 또는 주소 `#demo`
- 나이스 과목 → MYP 과목군 (예시 기준, 학교 확정 필요)

| MYP 과목군 | 나이스 과목 |
|---|---|
| 디자인 | 기술·가정 |
| 언어와 문학 | 국어 |
| 언어 습득 | 영어, 중국어 |
| 개인과 사회 | 도덕, 역사 |
| 수학 | 수학 |
| 과학 | 과학 |
| 체육과 보건 | 체육 |
| 예술 | 음악, 미술 |

- 규칙 형태를 골고루 보여 주도록 구성: 영역 1개 → 기준 2개(추정 분할), 영역 1개 → 기준 1개(배점 구간), 같은 기준을 여러 영역에서 평가(수학 C, 평균), 과목별 담당 교사·과목 의견

## 5. 기술

- 엑셀(xlsx) 읽기: SheetJS를 브라우저에서 사용, 서버 전송 없음
- 배포: GitHub Pages 유지

## 6. 남은 질문

확정: 1(메뉴·형식), 2(파일 모양), 3(학생 식별·학년도), 4(영역명은 "(만점 …)" 앞부분, 띄어쓰기 무시 비교)

1. 결시·미응시·인정점이 나이스 파일에 어떤 값으로 들어오는지 (현재: 숫자가 아니면 "확인 필요"로 표시)
2. 평가계획의 **기타 배점**(기본 점수 9, 백지·미응시 8, 장기 미인정 결석 7)을 수준으로 어떻게 바꿀지. 9점은 B2+D7=수준 0·0으로 자동 처리되지만 8·7점은 조합이 없어 수동 수정 필요
3. 지필평가 파일 모양 (도덕은 지필이 MYP에 반영되지 않아 아직 샘플 없음)
4. "합산" 방식이 필요한 과목이 있는지
5. 나이스 과목명 → MYP 과목군 대응 (도덕은 개인과 사회로 가정)
6. 성적표에서 뺄 과목 → 템플릿 과목 시트에 없으면 자동으로 빠짐
7. 성적표 머리글 정보 (현재: 학교명, 제목, 학년도·학기, 발급일, 서명란 2개를 앱에서 입력)
8. 점수 차 최소 규칙이 수준 차를 크게 만드는 경우가 있음. 예: 19점 → B10·D9 (수준 8·2). 수준 차가 가장 작은 조합(B8·D11 → 6·4)이 나은지

## 7. IB 기준 검토 결과 (2026-09-17)

근거: IB, *MYP: From principles into practice* (2014, 2022년 8월 개정) 중 Approaches to assessment, Reporting student achievement, MYP general grade descriptors

### 고친 오류

| 항목 | 이전 | IB 기준 | 변경 |
|---|---|---|---|
| 기준 일부만 평가한 과목 | 32점으로 환산해 등급 | 등급은 그 과목의 **모든 Criterion 수준**으로 정해야 함 | 기본은 수준만 보고, 환산 등급은 학교 방침 설정으로 분리 |
| 근거 합치기 기본값 | 평균 | 학기 점수 평균으로 성적을 내는 것은 부적절. 근거의 흐름(향상 추세)·일관성을 보고 교사가 판단(best-fit) | 기본값을 "일관된 수준"(최빈값, 동률이면 최근)으로, 계산값을 "제안값"으로 안내 |
| 등급 설명 | 의역한 문구 + 임의 영문 라벨(Very limited 등) | IB 일반 등급 설명 원문 | 원문을 충실히 옮긴 한국어로 교체, 영문 라벨 대신 총점 구간 표시 |

### 보강한 내용

- MYP 연차 표시 (중3 → MYP Year 3)
- 과목별 담당 교사 (나이스 파일의 "교과담당교사"에서 읽음), 과목별 의견 (3단계에서 작성)
- 템플릿 화면에 Criterion별 평가 횟수 표시 (IB: 모든 Criterion의 모든 세부 요소를 1년에 2번 이상 평가)
- 완성본 예시에 디자인 과목군(기술·가정) 추가

### 학교 운영 방침 (2026-09-17 결정)

학교 여건상 채점 기준표나 나이스 입력 방식을 IB 원칙대로 바꾸기 어려우므로, 교사 부담을 줄이는 **편의 기능을 기본으로 유지**한다. 시간을 들일 수 있는 교사는 3단계 검토에서 학생별로 직접 조정한다.

- **추정 분할 유지**: 영역 합계를 기준별 점수 차가 가장 작은 조합으로 나누고 "추정"으로 표시. 나이스에 영역을 기준별로 나누어 입력한 과목은 추정 없이 계산됨
- **수준 범위는 높은 쪽 기본**: 7~8 → 8. 설정에서 낮은 쪽 선택 가능, 학생별 수정 가능

### 참고: IB 원칙과의 차이 (앱은 편의 기능 유지)

- **추정 분할**: IB는 다른 성적 체계의 점수를 MYP 등급으로 바꾸는 것을 허용하지 않음. 채점 기준표가 MYP 수준 서술문으로 채점한 것이라 수준 역산은 가능하지만, 영역 합계를 나누는 추정은 근거를 만들어 내는 셈. IB 원칙상으로는 나이스에서 영역을 Criterion별로 나누어 입력하는 방식이 정확함
- **수준 7~8 같은 범위**: IB는 교사가 서술문에 비추어 낮은 쪽·높은 쪽을 판단. IB 원칙상으로는 채점 기준표에서 수준마다 배점을 따로 두는 방식이 정확함
- **내부 표준화**: 같은 과목을 여러 교사가 가르치면 최종 수준을 정하기 전에 채점 기준을 맞추는 협의가 필수
- **모든 strand 평가**: 수준을 정하려면 그 Criterion의 모든 세부 요소(strand)가 평가되어야 함
- **등급 경계**: 1–5, 6–9, 10–14, 15–18, 19–23, 24–27, 28–32는 IB가 MYP 모든 연차에 쓰도록 안내하는 grade boundary guidelines

### 앞으로 검토할 만한 것

- 봉사 활동(service as action) 기록
- 언어 습득 과목의 단계(Phase 1~6) 표시 (지금은 성적표 표시명에 "영어 (Phase 3)"처럼 적을 수 있음)
- 국가 성취도(A~E)를 함께 표시 (IB는 MYP 등급을 먼저 정한 뒤 다른 체계로 바꾸는 것은 허용)

## 8. 추가 기록: ATL · 학습자상 · 공동체 프로젝트 (2026-09-17, 모두 선택 기능)

켜고 끄기: 3단계 "추가 기록" 또는 4단계 출력 옵션. 켜면 3단계 "보기" 목록에 입력 화면이 생기고 성적표에 출력된다. 입력한 항목이 없는 학생은 성적표에서 해당 영역을 생략한다.

| 기능 | 입력 | 성적표 | IB 근거 |
|---|---|---|---|
| ATL 기능 | 학생 × 5개 기능 범주(기본) 또는 10개 군집. N·L·P·E(또는 1~4, 초·학·실·전) 입력, 열별 반 전체 일괄 입력, 엑셀 붙여넣기 | 기능별 발달 단계 표(초보자·학습자·실천가·전문가) | ATL은 공식 평가하지 않고 발달 단계로 피드백 (From principles into practice) |
| 학습자상 | 10가지 특성 중 두드러진 것 체크 + 메모 | 10가지 중 고른 특성 강조 + 메모 | 학습자상은 점수로 평가하지 않음 |
| 공동체 프로젝트 | **3학년만**. 제목, 지도 교사, 기준별 수준 0~8 (엑셀 붙여넣기 가능) | 제목·지도 교사·기준별 수준·총점, 등급은 선택 | MYP 3·4년차에서 끝나는 학교의 마무리 프로젝트 |

- 공동체 프로젝트 평가 기준은 자료마다 달라 두 체계를 선택하게 함: 3개 기준(계획하기·기능 적용하기·성찰하기, 24점) / 4개 기준(탐구하기·계획하기·실행하기·성찰하기, 32점). **학교에서 쓰는 IB 프로젝트 가이드 판 확인 필요**
- 공동체 프로젝트 1~7 등급 경계는 참고값(3개 기준: 1–4, 5–7, 8–11, 12–14, 15–18, 19–21, 22–24 / 4개 기준: MYP 일반 경계)이며 앱에서 수정 가능
- 완성본 예시는 세 기능을 모두 켠 상태 (공동체 프로젝트는 번호 순 3명씩 한 모둠)
